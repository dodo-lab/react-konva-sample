import React from 'react';
import SlideGenerator from './SlideGenerator';
import { Box } from '@mui/material';

const App: React.FC = () => (
  <Box m={4}>
    <SlideGenerator/>
  </Box>
);

export default App;
